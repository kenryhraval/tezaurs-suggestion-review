package lv.tezaurs.suggestions.suggestion.entity;

/** Human-selected progress status for a suggestion. */
public enum SuggestionStatus {
    NEW,
    IN_PROGRESS,
    POSTPONED,
    NEEDS_EXPERT,
    COMPLETED,
    GARBAGE
}
